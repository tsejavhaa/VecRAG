# ── Core ──────────────────────────────────────────────────────────
streamlit>=1.30.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-ollama>=0.1.0          # OllamaLLM for Ask tab
langchain-chroma>=0.1.0
langchain-experimental>=0.0.60    # SemanticChunker
chromadb>=0.5.0
requests>=2.31.0                 # Ollama connectivity check
sentence-transformers>=3.0.0
pymupdf>=1.23.0
pypdf>=4.0.0

# ── Optional: OCR for scanned PDFs ───────────────────────────────
# Uncomment + install Tesseract on your OS before using:
#   macOS:   brew install tesseract
#   Ubuntu:  sudo apt install tesseract-ocr
# pytesseract>=0.3.10
# pdf2image>=1.17.0

# ── Optional: Ollama provider ─────────────────────────────────────
# No extra pip package — install from https://ollama.com
# then: ollama pull nomic-embed-text